# App 模块说明

## 这个目录是干嘛的

`App` 用来放项目的业务流程，也就是“各个模块该按什么顺序配合工作”。

## 建议放什么

- 采样流程控制
- 状态机
- 任务调度
- 采样后如何触发算法
- 算法结果如何上报

## 以后可以新增的文件示例

- `rain_capture_app.c`
- `rain_capture_app.h`

## 适合写什么逻辑

例如：

- 初始化完成后开始采样
- 一帧数据采满后触发特征提取
- 算法判断完成后通过串口发送结果

## 一句话理解

`App` 负责“让各个模块协同工作”。
